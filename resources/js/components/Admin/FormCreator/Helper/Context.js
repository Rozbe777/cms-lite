import {createContext} from "react";

export const FormContext = createContext({})
export const FormContextEmail = createContext({})
export const FormContextMini = createContext({})
export const FormContextMobile = createContext({})
export const FormContextMultiSelected = createContext({})
export const FormContextNumber = createContext({})
export const FormContextFile = createContext({})
export const FormContextTextArea = createContext({})
export const FormContextUrl = createContext({})
export const FormContextYesNo = createContext({})
export const FormTheme = createContext({})
