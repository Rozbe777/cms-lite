class HelperClassFetures {
    constructor(data) {
        this.data = data;
    }
    normalizees(){
        return data;
    }
}

export const Normalize = (data) => {
    var Helpers = new HelperClassFetures(data)
    Helpers.normalizees();
}
